

model `openrouter/free`
- a special model
- a router that selects free models at random from the models available on OpenRouter.
- The router smartly filters for models that support features needed for your request such as image understanding, tool calling, structured outputs and more. 
# Limit
- embedding models are not currently supported.
  - No embedding endpoints in OpenRouter's API
